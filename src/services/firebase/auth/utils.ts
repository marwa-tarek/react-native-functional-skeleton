// Format firebase Auth exceptions to readable error message to user and log only unhandled exceptions (general errors) using Rollbar
export const getLocalError = (error: any, label: string): string => {
  //Todo return error when auth fails
  return ""
}
