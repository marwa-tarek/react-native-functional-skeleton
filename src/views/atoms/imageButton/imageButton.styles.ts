import { StyleSheet } from "react-native"
import { ViewStyleSheet } from "@yourProjectName/utils"

const viewStyles = ViewStyleSheet({})

const styles = StyleSheet.create({
  ...viewStyles,
})

export default styles
